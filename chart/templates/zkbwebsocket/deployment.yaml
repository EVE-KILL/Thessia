{{- if .Values.zkbwebsocket.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: zkbwebsocket
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "thessia.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.zkbwebsocket.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: zkbwebsocket
      {{- include "thessia.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: zkbwebsocket
        {{- include "thessia.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "thessia.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.zkbwebsocket.securityContext | nindent 8 }}
      containers:
        - name: zkbwebsocket
          image: "{{ .Values.global.image.repository }}:{{ .Values.global.image.tag }}"
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          command:
            - /bin/sh
            - -c
            - |
              ./bin/console zkbWebsocketKills
          resources:
            {{- toYaml .Values.zkbwebsocket.resources | nindent 12 }}
          envFrom:
            - configMapRef:
                name: config
            - secretRef:
                name: secrets
{{- end }}
