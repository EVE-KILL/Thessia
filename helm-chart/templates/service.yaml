apiVersion: v1
kind: Service
metadata:
  name: next-application
  labels:
    app: next-application
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
  selector:
    app: next-application
